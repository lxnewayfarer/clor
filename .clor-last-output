Both fixes are done:

1. **`agent.go`** — removed the `os.WriteFile(workdir+"/.clor-last-output", ...)` block entirely. Agent output is already stored in the proper run log.

2. **`web/index.html`** — added `tempFiles:['plan.md']` to the Architect preset using camelCase (matching how the UI stores node data), so new Architect nodes will have `plan.md` pre-populated in the temp files field and the orchestrator's `cleanupTempFiles()` will remove it after the run.
